pid /tmp/nginx-wasm-test.pid;
error_log /dev/stderr warn;

events {
    worker_connections 16;
}

http {
    include /etc/nginx/includes/mime.types;
    default_type application/octet-stream;
    access_log off;

    server {
        listen 127.0.0.1:18080;
        root /test;
    }
}
